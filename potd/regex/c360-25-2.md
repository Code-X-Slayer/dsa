# 🟢 Replace Spaces with `@40`

## 🔖 Tags

`#string` `#regex` `#java` `#easy`

---

## 📌 Problem

Given a string **STR**, replace all spaces between words with:

```
@40
```

---

## 🧠 Key Idea

Instead of replacing one space at a time:

Use **Regular Expression** to replace:

```
" +"  →  one or more spaces
```

This ensures:

* Multiple spaces are treated as one group
* Clean replacement
* No manual iteration needed

---

## 🧱 Approach Used in Code

### 1️⃣ Trim the String

Removes leading and trailing spaces:

```java
str.toString().trim()
```

---

### 2️⃣ Regex Pattern

```java
Pattern pattern = Pattern.compile(" +");
```

Meaning:

```
" +" → match one or more spaces
```

---

### 3️⃣ Replace All Matches

```java
matcher.replaceAll("@40")
```

All space groups → replaced with `"@40"`

---

## 💻 Code

```java
import java.util.* ;
import java.util.regex.*;
import java.io.*; 

public class Solution {
	public static StringBuilder replaceSpaces(StringBuilder str) {
		StringBuilder res = new StringBuilder();

		// Match one or more spaces
		Pattern pattern = Pattern.compile(" +");
		Matcher matcher = pattern.matcher(str.toString().trim());

		// Replace with @40
		res.append(matcher.replaceAll("@40"));

		return res;
	}
}
```

---

## 🧪 Example

### Input

```
Coding Ninjas Is A Coding Platform
```

### Output

```
Coding@40Ninjas@40Is@40A@40Coding@40Platform
```

---

## ⏱ Complexity

```
Time  : O(n)
Space : O(n)
```

Where `n = length of string`

---

## 🎯 One-Line Memory Trick

> Trim → Match `" +"` → Replace with `@40` → Done.
